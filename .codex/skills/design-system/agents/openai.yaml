interface:
  display_name: "Design System"
  short_description: "Make Codex follow DESIGN.md."
  brand_color: "#C96442"
  default_prompt: "Use $design-system to read DESIGN.md first, then make this UI follow its rules."
policy:
  allow_implicit_invocation: true
